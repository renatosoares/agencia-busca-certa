CREATE TABLE `desencontros` (
  `primeiro_nome` VARCHAR(30),
  `segundo_nome` VARCHAR(30),
  `email` VARCHAR(50),
  `ultimo_lugar` varchar(30),
  `ultimo_contato` VARCHAR(30),
  `qual_motivo` VARCHAR(30),
  `descreva_pessoa` VARCHAR(100),
  `liga_pessoa` VARCHAR(100),
  `reencontro_desfavoravel` VARCHAR(10),
  `mais` VARCHAR(100)

);

